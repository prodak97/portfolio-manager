/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind v4 uses CSS-based configuration via @source directive
  // See src/index.css for content paths
  theme: {
    extend: {},
  },
  plugins: [],
}